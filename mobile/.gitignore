node_modules/
.expo/
dist/
web-build/
.DS_Store
*.log
*.tsbuildinfo

# package-lock.json — EAS resolves fresh on each build (matching working build conditions)
package-lock.json

# FCM config — written by CI from GOOGLE_SERVICES_JSON secret
google-services.json

# Native
android/
ios/

# Local secrets
.env
.env.local

# EAS local credentials — never commit
credentials.json
